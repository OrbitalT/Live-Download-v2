--ignore-errors
--no-overwrites
--continue
# Archive Settings
--download-archive ./data/youtube-dl-archive-mp4.txt
--yes-playlist
# Uniform Format
--prefer-ffmpeg
--merge-output-format mp4
--format "bestvideo[ext=mp4]+bestaudio[ext=m4a]/bestvideo[ext=webm]+bestaudio[ext=webm]/bestvideo[ext=mp4]+bestaudio[ext=webm]/bestvideo+bestaudio/best"
# Get metadata
--add-metadata
# Error handling
--retries 5
